# TSC 1.3.8 / SPT 4.1.4 validation

The September 5, 2026 gameplay candidate was built from
`a336a35a85691629dc27342cbf12dd9855e6c5d6`, then installed and checked on SPT
4.1.4 / EFT 0.16.9.5.40743. Public release preparation updates documentation
and reconciles the existing GitHub port history; it preserves that candidate's
runtime source and game assets. Published artifact identity belongs to the
matching release's checksums and build/content evidence.

| Check | Result |
| --- | --- |
| Full local verification | 216 regression tests pass; five projects build with zero errors and four existing warnings. |
| Package | 169 files, four matching TSC DLLs, eight pinned bundles; extracted file hashes verified. |
| Pilot migration | Seven tests execute production migration code, including progress preservation, idempotency, future quest defaults, identity collision, save failure and cancellation. |
| Pilot shop and portrait | Four source/data contracts check offer terms, registration/import ordering, stock preservation and packaged portrait routing. |
| Installed server | TSC 1.3.8 loads and saves the existing locked Pilot entry as unlocked. |
| Native trader reads | Five requests pass 22 checks: Pilot's one Uplink offer is 50,000 RUB at LL1, maximum five; Jaeger has no Uplink listing; served portrait exactly matches the supplied PNG. |
| Profile preservation | Inventory, quests, cargo delivery data and Pilot/BTR progress match the stopped-server backup. Native restock timestamps are excluded from progress comparisons. |
| A-10 targeting | Solver/replay regression coverage plus user-reported solo gameplay improvement after the short-impact correction. This is not a matched multiplayer acceptance run. |
| Phone and store | User reported that the revised phone felt responsive with no observed issues. The pre-raid card/detail store and replacement icons were exercised in user screenshots. |
| Bottom-bar shortcut | Eleven additional fixture checks on v1.3.7 exercised production footer/lifecycle code using the installed native UI contract. Final rendering across resolutions remains a live check. |

The regression suite is proprietary-free. Tests using stubs or source
contracts are identified above; they do not run Unity's renderer or a complete
raid. Server trader reads use the existing authenticated profile and do not
submit a purchase, create a profile or manually edit its data.

## Remaining live acceptance

- Final Pilot portrait framing and a normal paid Uplink purchase in Trading.
- Bottom-bar appearance and navigation at different resolutions/UI scales.
- Broader maps, repeat raids, UAV, extraction and cargo payment/cancellation.
- Matched Fika human-host/client and dedicated-headless cases, including
  lethal damage/corpse synchronization and transactional settlement.
- Non-host/headless cargo remains intentionally unavailable pending the
  authoritative native item-price path; dedicated-headless A-10 is experimental.

See [known issues](../known-issues.md) and [Fika validation](../fika.md).
Earlier [4.1.4 port results](../port/SPT-4.1.4-VALIDATION.md) remain historical
and should not be read as the status of this later candidate.
